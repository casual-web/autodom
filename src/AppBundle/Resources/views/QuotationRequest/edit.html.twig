{% extends "AppBundle:Admin:base.html.twig" %}

{% block body %}

    <div class="container">
        <div class="page-header">
            <h1>Edition d'une demande de devis</h1>
        </div>
        {{ form(edit_form) }}

        <ul class="record_actions">
            <li>
                <a href="{{ path('admin_devis') }}">
                    Retour à la liste
                </a>
            </li>
            <li>{{ form(delete_form) }}</li>
        </ul>
    </div>
{% endblock %}
