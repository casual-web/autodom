{% extends "AppBundle:Admin:base.html.twig" %}

{% block body -%}
    <div class="container">
        <div class="page-header">
            <h1>Création d'un nouveau service</h1>
        </div>

        {{ form(form) }}
        <ul class="record_actions">
            <li>
                <a class="btn btn-default" role="button" href="{{ path('admin_service') }}">
                    Retour à la liste
                </a>
            </li>
        </ul>
    </div>
{% endblock %}
